export enum RestaurantOrderStatus {
  New = 'new',
  Preparing = 'preparing',
  Ready = 'ready',
  Delivered = 'delivered',
  WaitingPayment = 'waiting_payment',
  Paid = 'paid',
  Cancelled = 'cancelled',
}
